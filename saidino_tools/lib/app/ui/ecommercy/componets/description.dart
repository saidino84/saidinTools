import 'package:flutter/material.dart';

class EcmmerceItemDescription extends StatelessWidget {
  const EcmmerceItemDescription({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("lorem pissum sativum"),
      ),
    );
  }
}
